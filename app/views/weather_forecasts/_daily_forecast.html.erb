<p id="<%=dom_id(daily_forecast)%>">
  <strong><%= l(daily_forecast.date, format: :weekday) %></strong>
  <br>
  <%= daily_forecast.maximum_temperature %> (<%= raw han(daily_forecast, :maximum_temperature) %>)
  <br>
  <%= daily_forecast.precipitation_probability %> (<%= han(daily_forecast, :precipitation_probability) %>)
</p>
